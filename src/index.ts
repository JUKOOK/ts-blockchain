const sayHi = (name: string, age: number, gender: string): string => {
  return `안녕하세요 저는 ${name}입니다. 나이는 ${age}살이고, 성별은 ${gender}입니다.`;
};

console.log(sayHi("JUKOOK", 28, "female"));

// export {};
